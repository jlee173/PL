int index;
int i = 42;
int ints[3];


on akey
{
    for (index = 0; index < 3; index = index + 1)
    {
        ints[index] = index;
    }
}

on skey
{
    i = index + ints[0];
    print("i = " + i);
}
